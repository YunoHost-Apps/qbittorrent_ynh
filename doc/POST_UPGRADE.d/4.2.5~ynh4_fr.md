{% set url = "https://" + domain + ("" if path == "/" else path) -%}

## Ce qui change

`{{ url }}/api` était accessible sans connexion. Ce chemin est maintenant réservé aux administrateurs, comme le reste de l'interface web.

Les applications de ce serveur (Sonarr, Radarr…) joignent qBittorrent directement sur `http://127.0.0.1:{{ port }}`, sans mot de passe. Les clients d'une autre machine utilisent `{{ url }}/ext`, nom d'utilisateur `admin`, avec un mot de passe vérifié par qBittorrent.

{% if password %}qBittorrent n'avait pas de mot de passe propre, ou était resté sur le `adminadmin` par défaut : un mot de passe a été généré.

**{{ password }}**

Mettez à jour vos clients externes. Vous pouvez le changer dans *Outils > Options > Interface Web*, et le retrouver dans la documentation d'administration de cette application.{% else %}Votre mot de passe qBittorrent est inchangé : utilisez-le dans vos clients externes, pointés sur `/ext`.{% endif %}

Si votre instance était ouverte sur Internet, un coup d'œil à *Outils > Options > Téléchargements > Exécuter un programme externe* et à votre liste de torrents ne fait pas de mal.
