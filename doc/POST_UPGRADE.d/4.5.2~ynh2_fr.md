{% set url = "https://" + domain + ("" if path == "/" else path) -%}

La configuration et la liste des torrents de qBittorrent sont maintenant dans
`{{ install_dir }}/qBittorrent`, hors de portée de l'intégration multimédia que d'autres
applications mettent en place. Vos téléchargements n'ont pas bougé.
{% if legacy_profile %}
L'ancienne copie est encore dans `{{ legacy_profile }}` — supprimez-la quand votre liste
de torrents vous paraît correcte.{% endif %}
