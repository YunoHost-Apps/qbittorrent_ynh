{% set url = "https://" + domain + ("" if path == "/" else path) -%}

qBittorrent's config and torrent list now live in `{{ install_dir }}/qBittorrent`, out of
reach of the multimedia integration other apps set up. Your downloads have not moved.
{% if legacy_profile %}
The old copy is still in `{{ legacy_profile }}` — delete it once your torrent list looks right.{% endif %}
