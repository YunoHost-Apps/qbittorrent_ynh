{% set url = "https://" + domain + ("" if path == "/" else path) -%}

qBittorrent's config and torrent list moved out of the data directory, where other apps'
multimedia integration could reach them. Your downloads have not moved.
{% if legacy_profile %}
The old copy is still in `{{ legacy_profile }}` — delete it once your torrent list looks right.{% endif %}
